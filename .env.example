# Optional AI-assisted profile drafting. Keep this false on public reference deployments.
PROFILE_IMPORT_ENABLED=false

# Optional model override for Vercel AI Gateway. The function defaults to openai/gpt-5.4.
# PROFILE_DRAFT_MODEL=openai/gpt-5.4

# Required only when running the AI endpoint outside Vercel. Never commit a real value.
# AI_GATEWAY_API_KEY=
